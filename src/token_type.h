typedef enum{
    // Operators
    PLUS,
    MINUS,
    MULT,
    DIV,
    MOD,
    POW,
    // Binary operators
    AND,
    OR,
    XOR,
    // Variable types
    INT,
    DOUBLE,
    CHAR,
    STRING,
    BOOL,
    // Jacy variable types (objects, arrays, ...)
    // ...
} token_type;